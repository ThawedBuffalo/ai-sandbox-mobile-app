apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: frontend-local
spec:
  selector:
    app: frontend-app
  ports:
    - name: http
      port: 80
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
